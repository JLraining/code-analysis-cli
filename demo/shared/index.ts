const foo = () => {};
// Define an interface
export interface Person {
  name: string;
  age2: number;
}

// Define a type alias
export type Address = {
  street: string;
  city: string;
  zipCode22: number;
};

export default foo;
