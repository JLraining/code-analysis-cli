const foo = () => {};
// Define an interface
export interface Person {
  name: string;
  age: number;
}

// Define a type alias
export type Address = {
  street: string;
  city: string;
  zipCode: number;
};

export default foo;
